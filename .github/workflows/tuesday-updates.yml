name: Tuesday updates

on:
  schedule:
    # Once every Tuesday at 8 PM
    - cron: 0 20 * * 2
  push:
    branches:
      - main

jobs:
  updates:
    name: Update README
    runs-on: ubuntu-latest
    steps:
      - name: Update this repo's README
        uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://davidjourno.dev/rss.xml
          readme-section: posts
          empty-commits: "false"
